// Dotnet IL obfuscation worker — future implementation.
// Invoked as subprocess by penumbra.dotnet.il_worker.
// Uses dnlib for IL manipulation (rename, string-encrypt, flow, strip-debug).

using System;

Console.WriteLine("dotnet-worker: not yet implemented");
Environment.Exit(1);
